diff --git a/hipamd/hip-config.cmake.in b/hipamd/hip-config.cmake.in
index 9592b02d6..d9bc45080 100644
--- a/hipamd/hip-config.cmake.in
+++ b/hipamd/hip-config.cmake.in
@@ -89,21 +89,31 @@ set_and_check( hip_INCLUDE_DIRS "${hip_INCLUDE_DIR}" )
 set_and_check( hip_LIB_INSTALL_DIR "@PACKAGE_LIB_INSTALL_DIR@" )
 set_and_check( hip_BIN_INSTALL_DIR "@PACKAGE_BIN_INSTALL_DIR@" )
-if("@HIP_INSTALLS_HIPCC@")
-  if (WIN32)
-    set_and_check(hip_HIPCC_EXECUTABLE "${hip_BIN_INSTALL_DIR}/hipcc.exe")
-    set_and_check(hip_HIPCONFIG_EXECUTABLE "${hip_BIN_INSTALL_DIR}/hipconfig.exe")
-  else()
-    set_and_check(hip_HIPCC_EXECUTABLE "${hip_BIN_INSTALL_DIR}/hipcc")
-    set_and_check(hip_HIPCONFIG_EXECUTABLE "${hip_BIN_INSTALL_DIR}/hipconfig")
-  endif()
+if(WIN32)
+  set(_hip_hipcc_executable "${hip_BIN_INSTALL_DIR}/hipcc.exe")
+  set(_hip_hipconfig_executable "${hip_BIN_INSTALL_DIR}/hipconfig.exe")
+else()
+  set(_hip_hipcc_executable "${hip_BIN_INSTALL_DIR}/hipcc")
+  set(_hip_hipconfig_executable "${hip_BIN_INSTALL_DIR}/hipconfig")
+endif()
+
+if(EXISTS "${_hip_hipcc_executable}")
+  set(hip_HIPCC_EXECUTABLE "${_hip_hipcc_executable}")
+endif()
+if(EXISTS "${_hip_hipconfig_executable}")
+  set(hip_HIPCONFIG_EXECUTABLE "${_hip_hipconfig_executable}")
 endif()
+
+unset(_hip_hipcc_executable)
+unset(_hip_hipconfig_executable)
 
 if(NOT DEFINED HIP_PLATFORM)
-  if(NOT DEFINED ENV{HIP_PLATFORM})
+  if(DEFINED ENV{HIP_PLATFORM})
+    set(HIP_PLATFORM $ENV{HIP_PLATFORM} CACHE STRING "HIP Platform")
+  elseif(DEFINED hip_HIPCONFIG_EXECUTABLE)
     execute_process(COMMAND ${hip_HIPCONFIG_EXECUTABLE} --platform
       OUTPUT_VARIABLE HIP_PLATFORM
       OUTPUT_STRIP_TRAILING_WHITESPACE)
   else()
-    set(HIP_PLATFORM $ENV{HIP_PLATFORM} CACHE STRING "HIP Platform")
+    set(HIP_PLATFORM "@HIP_PLATFORM@")
   endif()
 endif()
@@ -125,11 +135,13 @@ set( hip_LIBRARY ${hip_LIBRARIES})
 
 set(HIP_INCLUDE_DIR ${hip_INCLUDE_DIR})
 set(HIP_INCLUDE_DIRS ${hip_INCLUDE_DIRS})
 set(HIP_LIB_INSTALL_DIR ${hip_LIB_INSTALL_DIR})
 set(HIP_BIN_INSTALL_DIR ${hip_BIN_INSTALL_DIR})
 set(HIP_LIBRARIES ${hip_LIBRARIES})
 set(HIP_LIBRARY ${hip_LIBRARY})
-if("@HIP_INSTALLS_HIPCC@")
+if(DEFINED hip_HIPCC_EXECUTABLE)
   set(HIP_HIPCC_EXECUTABLE ${hip_HIPCC_EXECUTABLE})
+endif()
+if(DEFINED hip_HIPCONFIG_EXECUTABLE)
   set(HIP_HIPCONFIG_EXECUTABLE ${hip_HIPCONFIG_EXECUTABLE})
 endif()
